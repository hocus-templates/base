# This script will be executed at Dockerfile build time as root.
# You can use it to install additional tools in your environment.
# For example, uncomment the line below to install jq:

# apt-get update && apt-get install jq
